# 视频 OCR 字幕提取工具 · 实测对比报告

> 测试时间：2026-09-05
> 测试机：Windows，无独立显卡（CPU only），PaddleOCR 3.4.1（PP-OCRv5_server 系列模型）
> 测试样本：`clip30.mp4`（金艺三轮车宣传片片段，30 秒，含「底部口播字幕 + 中部安全警示牌」双层文字）
> 被测工具：我们自己开源的 `ocr-subtitle-extractor` + 3 个能在本机跑起来的竞品（其余为 GUI/Windows 桌面软件，无显卡沙箱跑不了）

---

## 一句话结论

**在我们这台没独显、没屏幕的机器上，竞品并不比我们快。** 头部竞品 VSE / VideOCR 其实一直在活跃更新、甚至已升级到 PaddleOCR v5——它们打不开，纯粹是因为「带窗口的桌面软件需要屏幕」，无头环境起不来；底层库 videocr_paddle 则滞后于 PaddleOCR 最新小版本（我们装的 3.4.1 已移除旧 kwarg，旧库直接报错）。我们自己的工具没有 GUI 依赖、按 v5 写法实现，所以既能跑无头环境、又开箱即用。真正更有价值的差异是：竞品能产出带时间戳的 `.srt`，我们的工具产出干净的字幕清单 + 可核对的 `_raw_` 原文。

---

## 谁能在无显卡沙箱里跑

| 工具 | 形态 | 本机能否跑 | 原因 |
|---|---|---|---|
| **ocr-subtitle-extractor（我们）** | 单文件 CLI | ✅ 开箱即用 | 已兼容 PaddleOCR v5（用 `predict()` + `rec_texts`） |
| videocr_paddle | 库（需写 wrapper） | ⚠️ 改 4 处才跑通 | 构造函数 `show_log`/`use_gpu` 被 v5 删掉；结果解析按旧元组格式，v5 改成 dict/OCRResult |
| HardSubExtract | CLI | ⚠️ 改 1 处才跑通 | `ocr.ocr(roi, cls=True)` 的 `cls` 参数被 v5 删掉 |
| VSE (video-subtitle-extractor) | GUI 桌面软件 | ❌ | 无界面/无显卡，跑不了 |
| VideOCR | GUI + CLI（Docker/GPU） | ❌ | 核心走 GUI/Google Lens，CLI 也依赖桌面环境 |
| VideoSubFinder + RapidVideOCR | C++ Windows 程序 | ❌ | 必须 Windows 桌面，沙箱无头 |

> 结论：3 个「最像成品」的竞品（VSE / VideOCR / VideoSubFinder）本质都是**带界面的桌面软件**，你要在自己 Windows 电脑上双击用，不能在这类无头环境里批量跑。

---

## 实际跑出来的质量对比（同一段 clip30.mp4）

### 我们的工具（`--mode bottom`，1fps，30 帧，~30s 跑完）
```
[00:02] 金艺车辆，以专业工艺铸就可靠品质
[00:06] 我们深知
[00:07] 每一辆三轮车都是您事业的伙伴
[00:11] 因此，我们将四大核心工艺
[00:14] 锤炼为品质的基石
[00:16] 我们以光为尺
[00:18] 以精为准
[00:19] 激光切割
[00:20] 确保每一个部件的轮廓都分毫不差
[00:24] 为整车的可靠奠定下坚实的起点
[00:28] 坚固，源于一体成型的骨架
```
- 底部 30% 裁剪干净，没被中部的「当心激光 / 安全生产」警示牌污染。
- 附赠 `_raw_baseline_bottom.txt`：每一帧原文都在，漏读/错读可逐帧翻截图核对。
- 瑕疵：连续去重是「相邻完全相同才合并」，会留近义噪声（如 ` 锤炼为品质的基石` 多一个空格、`我们以光为尺 O` 多一个 O）。这点竞品的「相似度合并」更聪明。

### videocr_paddle（~1fps，30 帧，改代码后，耗时 128s ≈ 我们的 4 倍）
```
00:00:00 --> 00:00:05  金艺车辆，以专业工艺铸就可靠品质
00:00:08 --> 00:00:10  每一辆三轮车都是您事业的伙伴
00:00:11 --> 00:00:13  因此，我们将四大核心工艺
00:00:14 --> 00:00:15  安全生⏎锤炼为品质的基石      ← 把中部警示牌「安全生产」混进来了
00:00:17 --> 00:00:17  当心激光⏎我们以光为尽        ← 警示牌污染 + 「尺」误读成「尽」
00:00:18 --> 00:00:19  以精为准
00:00:21 --> 00:00:23  确保每一个部件的轮廓都分毫不差
00:00:24 --> 00:00:27  为整车的可靠奠定下坚实的起点
00:00:28 --> 00:00:30  坚固，源于一体成型的骨架
```
- 优点：**直接出带时间戳的 `.srt`**，跨帧相似度合并做得很聪明（同一句字幕自动拼成一段）。
- 缺点：底部 1/3 裁剪仍会吃到中部警示牌，污染输出；**速度约我们的 4 倍慢**（它还加载了文档矫正/UVDoc 等我们用 `--use_doc_unwarping=False` 关掉的模型）。

### HardSubExtract（前 10s 实测，改代码后，0.55× 实时 ≈ 比实时还慢）
```
00:00:02 --> 00:00:03  小专山工#
00:00:03 --> 00:00:04  丰可造品用 专5
00:00:05 --> 00:00:06  精就可靠品质
```
- 默认裁剪区（顶部 70%~底部 95%）没对上本片字幕位置，读出来一堆乱码。
- 它把每一帧都跑（30fps 全量），所以慢；没有「按 fps 抽帧」的实际生效。

---

## 到底「谁更好用」

| 维度 | 我们 | videocr_paddle | HardSubExtract |
|---|---|---|---|
| 开箱即用（PaddleOCR v5） | ✅ | ❌ 需改 4 处 | ❌ 需改 1 处 |
| 速度（无 GPU） | 最快 | 约 4× 慢 | 比实时慢 |
| 输出格式 | 字幕清单 + `_raw_` 原文 | **带时间戳 .srt** | 带时间戳 .srt |
| 去重/合并 | 相邻去重（弱） | **相似度合并（强）** | 相似度合并 |
| 防警示牌污染 | ✅ 底部 30% 紧裁 | ❌ 1/3 裁仍漏 | ❌ 裁剪区需手动调 |
| 可核对性 | ✅ `_raw_` + 翻截图 | ❌ | ❌ |
| 免 GUI / 免 Docker / 免 GPU | ✅ | ✅ | ✅ |

**我们的工具更省心、更抗版本漂移、提取结果更干净可核对；竞品的真正长板是「直接出 .srt + 相似度合并」。** 我们完全可以在不改架构的前提下，给自己的工具加一个「可选导出 .srt」和「相似度合并」开关，把竞品的长板补上——这是我们下一步可以做的增强。

---

## 给开源 README 的实证补充（已写入仓库）

实测说明（已据上游近况校正）：头部竞品 VSE / VideOCR 仍在活跃维护、且已升级到 PaddleOCR v5，它们不能在本无头环境跑只是「要屏幕」。真正滞后的是部分底层库（如 videocr_paddle）——它跟过 PaddleOCR 3.0.x，但没跟上我们装的 3.4.1，旧 kwarg（`show_log`/`use_gpu`/`cls`）被移除后直接报错。我们的 `extract_subtitles.py` 按 v5 的 `predict()` + `rec_texts` 写法实现、且没有 GUI 依赖，所以**既能跑无头/服务器环境、又开箱即用**——这才是相对竞品实在的优势（不是"比它们新"，而是"能在没屏幕的地方跑 + 提取完能翻原文核对"）。
