---
name: ocr-subtitle-extractor
description: 视频 OCR 字幕提取与核实的完整流程。涵盖从视频→抽帧→OCR→精修→核实的所有步骤，ffmpeg 抽帧 + PaddleOCR，支持双层字幕与漏句复核。
agent_created: true
---

# 视频 OCR 字幕提取与核实流程

一个通用的「从视频里把字幕文字抠出来」工作流。核心脚本 `extract_subtitles.py`：ffmpeg 抽帧（OpenCV 保底）→ PaddleOCR → 连续去重 → 输出精修字幕 + 原始 OCR 两份文件。

## 一、提取字幕

```bash
python extract_subtitles.py --video <视频路径> --out <字幕输出.txt>
```

可选参数：
- `--mode bottom|full`：`bottom`（默认）只取画面底部 30%，适合顶部有 logo / 贴纸干扰的视频；`full` 抽整帧，适合**双层字幕**（见下）。
- `--fps N`：每秒抽帧数，默认 1（字幕变化慢时 1 足够；加密会翻倍 OCR 耗时）。
- `--frames-dir <目录>`：抽帧临时目录，默认与输出同目录下的 `_frames`。
- `--typo-map <json>`：错字修正表（见 `typoMap.example.json`），逐条 replace。
- `--crop <ffmpeg表达式>`：自定义裁剪区域，覆盖 `--mode`（高级用法）。

跑完自动生成两份文件（假设 `--out subs.txt`）：
- `subs.txt` — 精修字幕（连续相同去重后）
- `_raw_subs.txt` — 原始 OCR（每一帧都写，不过滤不去重）← **核实的唯一依据**

### ⚠️ 双层字幕（抖音类视频特有）

很多短视频字幕分**两层**，底部裁剪只能抓到一层：
- **底层白字**：口播字幕，通常在画面**底部** → `bottom` 能抓到
- **顶层/中层彩色「卖点标题卡」**：花字、加重卖点，通常在画面**顶部或中部** → `bottom` **必漏**

遇到这类视频，首次就直接用 `--mode full` 全帧提取，再人工滤掉品牌横幅 / UI 文字噪音（这些是常驻贴纸，不是字幕）。

## 二、精修规则

拿到原始输出后，按以下规则精修 `subs.txt`：

1. **删噪音**：明显不是字幕的碎片（水印、数字编号碎片）
2. **去重抖动**：同一句出现多次只保留正确版本（OCR 抖动会产生错字版，选对的删错的）
3. **去截断版**：同一句前半不完整 + 完整版，只保留完整版
4. **删型号/编号**：如 `XN1000DZH-C` 等，只保留正文
5. **合并断句**：一句被 OCR 拆成两帧（如「不止」+「不止于美更在于盾」），合并为一句
6. **修正错字**：对照前后帧取正确版
7. **适当加标点**：纯 OCR 无标点，加逗号/句号提升可读性

## 三、核实流程（⚠️ 关键：不要再重跑 OCR）

**目标**：确认精修版 `subs.txt` 没有遗漏任何原始内容。

### 步骤1：读两个文件
直接打开对比：
- `_raw_subs.txt` — 原始 OCR（每次跑脚本自动生成，不要重跑）
- `subs.txt` — 精修字幕

### 步骤2：出逐条对照表 + 查空白帧

| 帧 | 原始 OCR | 处理 | 结果 |
|---|---------|------|------|
| 00:01 | 原始文本 | 保留/去重/合并等 | ✅ 对应第 N 条 |
| ... | ... | ... | ... |

### ⚠️ 步骤3：翻空白帧截图

**原始 OCR 中连续空帧超过 2 秒（跳过 3 帧以上），必须翻对应截图看是否有遗漏字幕。** 包括视频开头（00:00 起）和结尾的空白——这些位置也常有字幕。

OCR 会漏读：低对比度、特殊字体、画面叠加文字都可能识别不到。**空白 ≠ 没字。**

翻截图方式：抽帧目录里的 `f_NNN.jpg`，`f_001` = 第 0 秒，依秒数找对应帧。

**结论格式**：X 条原始 → Y 条精修，无遗漏，无多删。列出删/去重/合并/修正的具体数量。

## 四、已知局限 & 改进方向

- **固定裁剪区域**：字幕在画面中间时会漏。已调研 VideoSubFinder / VSE，均不适合集成（封装太厚、精度不可控）。更实际的方案是用 `--crop` 手动指定裁剪位置。
- **错字修正依赖人工 / typoMap**：可维护一份 `typoMap.json` 自动修常见错字（见 `typoMap.example.json`）。
- **非字幕文字混入**：车标、贴纸、墙标会被 OCR 当字幕读入，当前靠人工删除。
- **多帧投票无效**（已实测）：同一字幕取 3 帧投票不能解决漏读，只增加耗时。投票能纠「读错」，纠不了「读不到」——漏读时 3 帧全空。
- **SSIM 跳帧无效**（已实测）：跳帧会导致漏内容，坚决不用。1fps 都能漏，跳帧只会漏上加漏。

## 五、常见错误（禁止重犯）

- ❌ **核实 ≠ 重跑 OCR**：原始数据已在 `_raw_*.txt` 里，直接读就行
- ❌ **不要写新脚本来做核实**：两个文件读进来对比就完了
- ❌ **OCR 漏读 ≠ 没有字**：空白帧超过 2 秒必须翻截图确认
- ❌ **不要只信文本不信截图**：截图是原始数据，OCR 文本是加工后的
- ✅ **核实 = 读已有数据 + 出对照表 + 空白帧翻截图**

## 六、抽帧方式（OpenCV → ffmpeg）

`extract_subtitles.py` 用 ffmpeg 1fps 抽帧为主、OpenCV 保底：

| | ffmpeg | OpenCV |
|---|---|---|
| 速度 | **快（约 1–2s / 90s 视频）** | 较慢（约 4s） |
| 画质 | 清晰 | 清晰 |
| 字幕 | 清楚 | 清楚 |

**注意点**：
- **保底机制**：ffmpeg 不存在 / 返回非 0 / 抽出 0 张帧 → 自动退回 OpenCV，抽帧不会因工具缺失而崩
- **默认裁剪底部 30%**：很多视频顶部有干扰文字（logo、贴纸），全帧喂 OCR 会把这些当字幕读入，输出直接废掉。`--mode full` 才抽整帧
- 输出 `f_%03d.jpg`（3 位补零）；下游按 `enumerate(files)` 定秒数，与编号无关
- `-q:v 2` 高质量 JPEG；`fps=1` 每秒 1 帧
- 每次抽帧前清空旧 jpg，防止多轮运行残留帧混入

## 七、运行环境关键坑

- **PaddleOCR v5 解析方式**：`r = ocr.predict(图片路径)[0]`，文本取 `r.rec_texts`（**对象属性**）。旧写法 `r.get('rec_texts')` 在 v5 下会抛 AttributeError 或返回空，导致整份提取全空。脚本已用兼容写法 `get_texts()`。
- **离线环境必设环境变量**：运行前加 `PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK=True`（bash 前缀 / PowerShell `set`），否则会卡在模型源连通性检查。
- **禁止并行跑多个 PaddleOCR**（已实测）：同一轮同时启动 2+ 个提取进程会各自加载模型 → 内存/资源挤崩 → raw 文件 0 字节、无报错。必须**串行逐个跑**。
- **OCR 多帧任务前台跑**：长视频 OCR 需数分钟，后台任务可能被超时 kill，建议前台执行并设足超时。

## 八、复核漏句工作流

觉得某条字幕漏句时，按此稳流程，不破坏成品：

1. **原成品一个字不动**。复制成新文件（如 `subs_全帧核对.txt`）再改，原文件留作对照。
2. **全帧重提做缺失对照**：用 `--mode full` 把该视频重提一遍（输出到不同 `--out`），拿到全帧 raw。
3. **对照找漏**：把全帧 raw 与成品逐条比，全帧里出现、成品里没有的「干净短句」就是疑似漏句。
4. **翻截图确认**：全帧帧图在 `--frames-dir`（`f_NNN.jpg`，`f_001`=第 0 秒）。逐张 Read 疑似漏句对应秒数的帧，确认屏幕上确有该文字再补。
5. **品牌横幅 / UI 噪音不补**：界面文字是贴纸 / 屏幕录屏，不是口播字幕，保持删除。
6. **补全版写新文件**，给用户对照；确认无误后由用户决定覆盖还是保留两套。
